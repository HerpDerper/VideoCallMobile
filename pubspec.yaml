name: flutter_video_call_mobile
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.17.0
  shared_preferences: ^2.0.15
  flutter_bloc: ^8.1.1
  dio: ^4.0.6
  get_it: ^7.2.0
  freezed: ^2.3.2
  build_runner: ^2.3.3
  freezed_annotation: ^2.2.0
  json_serializable: ^6.6.0
  videosdk: ^1.1.0
  http: ^0.13.5
  after_layout: ^1.2.0
  image_picker: ^0.8.4
  image_cropper: ^3.0.1
  sidebarx: ^0.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - images/icon.png